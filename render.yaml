# Render.com 部署設定檔
# 這個檔案告訴 Render 如何部署你的應用程式

services:
  - type: web
    name: teacher-quota-system
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
